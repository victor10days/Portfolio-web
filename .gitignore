node_modules/
dist/
.env

# Large root-level media (not part of the app)
/*.mov
/*.png
/*.JPG
/Screenshot*
FLYER*

# Database (rebuilt by seed on deploy). Globbed because iCloud sync on the
# Desktop produces "portfolio 2.db-wal" style duplicates.
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm

# OS files
.DS_Store
Thumbs.db

# Editor
*.code-workspace

# Design-tool memory (stamps live in src/index.css)
.hallmark/
